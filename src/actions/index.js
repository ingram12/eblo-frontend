import { createAction } from 'redux-actions';

export const loginUser = createAction('USER_LOGIN');
export const logoutUser = createAction('USER_LOGOUT');
export const signuoUser = createAction('USER_SIGNUP');
